From e3281d43c3e85e00ab305267d08a83b9a449cf50 Mon Sep 17 00:00:00 2001
From: omo50 <144749186+omo50@users.noreply.github.com>
Date: Fri, 26 Jun 2026 01:31:52 -0500
Subject: [PATCH 2/3] patch 0002: add --no-refresh flag and CommitChanges
 helper

---
 cmd/init.go           | 13 +------------
 cmd/pin.go            | 13 +------------
 cmd/rehash.go         | 17 ++---------------
 cmd/remove.go         | 13 +------------
 cmd/root.go           |  5 +++++
 cmd/serve.go          | 11 +----------
 cmd/update.go         | 13 +------------
 core/pack.go          | 17 +++++++++++++++++
 curseforge/detect.go  | 13 +------------
 curseforge/export.go  | 13 +------------
 curseforge/import.go  | 13 +------------
 curseforge/install.go | 13 +------------
 github/install.go     | 11 +----------
 modrinth/export.go    | 13 +------------
 modrinth/install.go   | 11 +----------
 url/install.go        | 13 +------------
 16 files changed, 37 insertions(+), 165 deletions(-)

diff --git a/cmd/init.go b/cmd/init.go
index d8cdd09..f610ba7 100644
--- a/cmd/init.go
+++ b/cmd/init.go
@@ -172,18 +172,7 @@ var initCmd = &cobra.Command{
 			fmt.Println(err)
 			os.Exit(1)
 		}
-		err = index.Write()
-		if err != nil {
-			fmt.Println(err)
-			os.Exit(1)
-		}
-		err = pack.UpdateIndexHash()
-		if err != nil {
-			fmt.Println(err)
-			os.Exit(1)
-		}
-		err = pack.Write()
-		if err != nil {
+		if err = core.CommitChanges(&index, &pack); err != nil {
 			fmt.Println(err)
 			os.Exit(1)
 		}
diff --git a/cmd/pin.go b/cmd/pin.go
index 9d70b7b..3899abe 100644
--- a/cmd/pin.go
+++ b/cmd/pin.go
@@ -41,18 +41,7 @@ func pinMod(args []string, pinned bool) {
 		fmt.Println(err)
 		os.Exit(1)
 	}
-	err = index.Write()
-	if err != nil {
-		fmt.Println(err)
-		os.Exit(1)
-	}
-	err = pack.UpdateIndexHash()
-	if err != nil {
-		fmt.Println(err)
-		os.Exit(1)
-	}
-	err = pack.Write()
-	if err != nil {
+	if err = core.CommitChanges(&index, &pack); err != nil {
 		fmt.Println(err)
 		os.Exit(1)
 	}
diff --git a/cmd/rehash.go b/cmd/rehash.go
index e5a3624..f37e86e 100644
--- a/cmd/rehash.go
+++ b/cmd/rehash.go
@@ -80,21 +80,8 @@ var rehashCmd = &cobra.Command{
 			os.Exit(1)
 		}
 
-		err = index.Write()
-		if err != nil {
-			fmt.Printf("Error writing index: %v\n", err)
-			os.Exit(1)
-		}
-
-		err = pack.UpdateIndexHash()
-		if err != nil {
-			fmt.Printf("Error updating index hash: %v\n", err)
-			os.Exit(1)
-		}
-
-		err = pack.Write()
-		if err != nil {
-			fmt.Printf("Error writing pack: %v\n", err)
+		if err = core.CommitChanges(&index, &pack); err != nil {
+			fmt.Printf("Error committing changes: %v\n", err)
 			os.Exit(1)
 		}
 	},
diff --git a/cmd/remove.go b/cmd/remove.go
index a8c9e9c..d8ccb12 100644
--- a/cmd/remove.go
+++ b/cmd/remove.go
@@ -42,18 +42,7 @@ var removeCmd = &cobra.Command{
 			fmt.Println(err)
 			os.Exit(1)
 		}
-		err = index.Write()
-		if err != nil {
-			fmt.Println(err)
-			os.Exit(1)
-		}
-		err = pack.UpdateIndexHash()
-		if err != nil {
-			fmt.Println(err)
-			os.Exit(1)
-		}
-		err = pack.Write()
-		if err != nil {
+		if err = core.CommitChanges(&index, &pack); err != nil {
 			fmt.Println(err)
 			os.Exit(1)
 		}
diff --git a/cmd/root.go b/cmd/root.go
index 4e016a9..2ddae1d 100644
--- a/cmd/root.go
+++ b/cmd/root.go
@@ -75,6 +75,11 @@ func init() {
 	var nonInteractive bool
 	rootCmd.PersistentFlags().BoolVarP(&nonInteractive, "yes", "y", false, "Accept all prompts with the default or \"yes\" option (non-interactive mode) - may pick unwanted options in search results")
 	_ = viper.BindPFlag("non-interactive", rootCmd.PersistentFlags().Lookup("yes"))
+
+	var noRefresh bool
+	rootCmd.PersistentFlags().BoolVar(&noRefresh, "no-refresh", false,
+		"Skip index and pack.toml refresh after modifications (use 'packwiz refresh' to finalize batch operations)")
+	_ = viper.BindPFlag("no-refresh", rootCmd.PersistentFlags().Lookup("no-refresh"))
 }
 
 // initConfig reads in config file and ENV variables if set.
diff --git a/cmd/serve.go b/cmd/serve.go
index 4de0b51..3712136 100644
--- a/cmd/serve.go
+++ b/cmd/serve.go
@@ -163,16 +163,7 @@ func doServeRefresh(pack *core.Pack, index *core.Index) error {
 	if err != nil {
 		return err
 	}
-	err = index.Write()
-	if err != nil {
-		return err
-	}
-	err = pack.UpdateIndexHash()
-	if err != nil {
-		return err
-	}
-	err = pack.Write()
-	if err != nil {
+	if err = core.CommitChanges(index, pack); err != nil {
 		return err
 	}
 	fmt.Println("Index refreshed!")
diff --git a/cmd/update.go b/cmd/update.go
index af17182..253f63c 100644
--- a/cmd/update.go
+++ b/cmd/update.go
@@ -195,18 +195,7 @@ var UpdateCmd = &cobra.Command{
 			}
 		}
 
-		err = index.Write()
-		if err != nil {
-			fmt.Println(err)
-			os.Exit(1)
-		}
-		err = pack.UpdateIndexHash()
-		if err != nil {
-			fmt.Println(err)
-			os.Exit(1)
-		}
-		err = pack.Write()
-		if err != nil {
+		if err = core.CommitChanges(&index, &pack); err != nil {
 			fmt.Println(err)
 			os.Exit(1)
 		}
diff --git a/core/pack.go b/core/pack.go
index 87a41dd..e61153e 100644
--- a/core/pack.go
+++ b/core/pack.go
@@ -153,6 +153,23 @@ func (pack Pack) Write() error {
 	return f.Close()
 }
 
+// CommitChanges persists the index and pack files to disk.
+// When the global --no-refresh flag is set, this is a no-op;
+// .pw.toml files remain on disk but the index is not rebuilt.
+// Run packwiz refresh to finalize after batch operations.
+func CommitChanges(index *Index, pack *Pack) error {
+	if viper.GetBool("no-refresh") {
+		return nil
+	}
+	if err := index.Write(); err != nil {
+		return err
+	}
+	if err := pack.UpdateIndexHash(); err != nil {
+		return err
+	}
+	return pack.Write()
+}
+
 // GetMCVersion gets the version of Minecraft this pack uses, if it has been correctly specified
 func (pack Pack) GetMCVersion() (string, error) {
 	mcVersion, ok := pack.Versions["minecraft"]
diff --git a/curseforge/detect.go b/curseforge/detect.go
index c067a3c..f4df6df 100644
--- a/curseforge/detect.go
+++ b/curseforge/detect.go
@@ -119,18 +119,7 @@ var detectCmd = &cobra.Command{
 			fmt.Println(err)
 			return
 		}
-		err = index.Write()
-		if err != nil {
-			fmt.Println(err)
-			return
-		}
-		err = pack.UpdateIndexHash()
-		if err != nil {
-			fmt.Println(err)
-			return
-		}
-		err = pack.Write()
-		if err != nil {
+		if err = core.CommitChanges(&index, &pack); err != nil {
 			fmt.Println(err)
 			return
 		}
diff --git a/curseforge/export.go b/curseforge/export.go
index 98e2d35..5e138d3 100644
--- a/curseforge/export.go
+++ b/curseforge/export.go
@@ -43,18 +43,7 @@ var exportCmd = &cobra.Command{
 			fmt.Println(err)
 			os.Exit(1)
 		}
-		err = index.Write()
-		if err != nil {
-			fmt.Println(err)
-			os.Exit(1)
-		}
-		err = pack.UpdateIndexHash()
-		if err != nil {
-			fmt.Println(err)
-			os.Exit(1)
-		}
-		err = pack.Write()
-		if err != nil {
+		if err = core.CommitChanges(&index, &pack); err != nil {
 			fmt.Println(err)
 			os.Exit(1)
 		}
diff --git a/curseforge/import.go b/curseforge/import.go
index f4f8a41..c00a7cf 100644
--- a/curseforge/import.go
+++ b/curseforge/import.go
@@ -353,18 +353,7 @@ var importCmd = &cobra.Command{
 			fmt.Println("No files copied!")
 		}
 
-		err = index.Write()
-		if err != nil {
-			fmt.Println(err)
-			os.Exit(1)
-		}
-		err = pack.UpdateIndexHash()
-		if err != nil {
-			fmt.Println(err)
-			os.Exit(1)
-		}
-		err = pack.Write()
-		if err != nil {
+		if err = core.CommitChanges(&index, &pack); err != nil {
 			fmt.Println(err)
 			os.Exit(1)
 		}
diff --git a/curseforge/install.go b/curseforge/install.go
index d5e71ec..4118185 100644
--- a/curseforge/install.go
+++ b/curseforge/install.go
@@ -244,18 +244,7 @@ var installCmd = &cobra.Command{
 			os.Exit(1)
 		}
 
-		err = index.Write()
-		if err != nil {
-			fmt.Println(err)
-			os.Exit(1)
-		}
-		err = pack.UpdateIndexHash()
-		if err != nil {
-			fmt.Println(err)
-			os.Exit(1)
-		}
-		err = pack.Write()
-		if err != nil {
+		if err = core.CommitChanges(&index, &pack); err != nil {
 			fmt.Println(err)
 			os.Exit(1)
 		}
diff --git a/github/install.go b/github/install.go
index 271f302..1a52f5b 100644
--- a/github/install.go
+++ b/github/install.go
@@ -204,16 +204,7 @@ func installRelease(repo Repo, release Release, regex string, pack core.Pack) er
 	if err != nil {
 		return err
 	}
-	err = index.Write()
-	if err != nil {
-		return err
-	}
-	err = pack.UpdateIndexHash()
-	if err != nil {
-		return err
-	}
-	err = pack.Write()
-	if err != nil {
+	if err = core.CommitChanges(&index, &pack); err != nil {
 		return err
 	}
 
diff --git a/modrinth/export.go b/modrinth/export.go
index f2bfceb..4742983 100644
--- a/modrinth/export.go
+++ b/modrinth/export.go
@@ -40,18 +40,7 @@ var exportCmd = &cobra.Command{
 			fmt.Println(err)
 			return
 		}
-		err = index.Write()
-		if err != nil {
-			fmt.Println(err)
-			return
-		}
-		err = pack.UpdateIndexHash()
-		if err != nil {
-			fmt.Println(err)
-			return
-		}
-		err = pack.Write()
-		if err != nil {
+		if err = core.CommitChanges(&index, &pack); err != nil {
 			fmt.Println(err)
 			return
 		}
diff --git a/modrinth/install.go b/modrinth/install.go
index f45c00a..01a0b2c 100644
--- a/modrinth/install.go
+++ b/modrinth/install.go
@@ -380,16 +380,7 @@ func installVersion(project *modrinthApi.Project, version *modrinthApi.Version,
 		return err
 	}
 
-	err = index.Write()
-	if err != nil {
-		return err
-	}
-	err = pack.UpdateIndexHash()
-	if err != nil {
-		return err
-	}
-	err = pack.Write()
-	if err != nil {
+	if err = core.CommitChanges(index, &pack); err != nil {
 		return err
 	}
 
diff --git a/url/install.go b/url/install.go
index 22a17cd..c0909bf 100644
--- a/url/install.go
+++ b/url/install.go
@@ -105,18 +105,7 @@ var installCmd = &cobra.Command{
 			fmt.Println(err)
 			os.Exit(1)
 		}
-		err = index.Write()
-		if err != nil {
-			fmt.Println(err)
-			os.Exit(1)
-		}
-		err = pack.UpdateIndexHash()
-		if err != nil {
-			fmt.Println(err)
-			os.Exit(1)
-		}
-		err = pack.Write()
-		if err != nil {
+		if err = core.CommitChanges(&index, &pack); err != nil {
 			fmt.Println(err)
 			os.Exit(1)
 		}
-- 
2.52.0.windows.1

