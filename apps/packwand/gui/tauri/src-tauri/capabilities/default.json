{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "Capability for the bundled boot page to select a workspace folder. The packwand gui server pages (http://127.0.0.1:*) are intentionally NOT granted IPC access; all pack management flows through the server's own HTTP API.",
	"windows": ["main"],
	"permissions": [
		"core:default",
		"dialog:allow-open",
		"allow-select-workspace",
		"allow-core-list-instances",
		"allow-core-plan-launch",
		"allow-launcher-boot",
		"allow-launcher-cancel",
		"allow-auth-login",
		"allow-auth-logout",
		"allow-auth-status"
	]
}
