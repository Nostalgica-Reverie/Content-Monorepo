[package]
name = "packwand-collab"
description = "Host-authoritative live collaboration sessions for the Packwand IDE"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
# Noise protocol. The invite PSK both authenticates the peer and keys the
# channel; see transport.rs. New to this workspace's lockfile.
snow = "0.10"
rand = "0.9"
base64 = "0.22"
