name: 'Setup Go'
description: 'Install the pinned Go toolchain used by monorepo Go components.'

runs:
  using: 'composite'
  steps:
    - name: Setup Go
      uses: https://github.com/actions/setup-go@v5
      with:
        go-version: '1.26.0'
        cache: false
