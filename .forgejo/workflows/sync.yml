name: "Sync"

on:
  workflow_dispatch:
  push:
    paths:
      - 'modpacks/simply-optimized-forked/**'
      - 'modpacks/rekindled-common/**'
      - 'modpacks/lce-common/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sync:
    uses: ./.forgejo/workflows/packwand-task.yml
    with:
      verb: workspace sync
      commit-message: 'actions: sync'
