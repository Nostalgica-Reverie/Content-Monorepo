when:
  - event: ['manual']

engine: microvm
image: nixos
dependencies: [bun, cmake, gcc, git, gnumake, just, ninja, pkg-config, rustup]

steps:
  - name: Build Packwand CLI
    command: cd apps/packwandrs && cargo build --locked --release -p packwand-cli
  - name: Type-check docs scripts
    command: just docs-typecheck
  - name: Build docs sites
    command: just docs-build
  - name: Check cross-site links
    command: bun docs/link-lint.mts
